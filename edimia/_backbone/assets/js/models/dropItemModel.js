define(['underscore', 'backbone'], function(_, Backbone) {

    var DropItemModel = Backbone.Model.extend({
        defaults: {
            object: ""
        },
        initialize: function() {
            
        },
        validate: function(attrs) {
            
        }
    });

    return DropItemModel;

});